@startuml StringExtensions.Decrypt
title StringExtensions.Decrypt
start
#LightBlue:var fullCipher = Convert.FromBase64String(cipherText)
var iv = new byte[16]
var cipher = new byte[16];
:Buffer.BlockCopy(fullCipher, 0, iv, 0, iv.Length);
:Buffer.BlockCopy(fullCipher, iv.Length, cipher, 0, iv.Length);
#LightBlue:var key = Encoding.UTF8.GetBytes(keyString)
string result
var aesAlg = Aes.Create()
var decryptor = aesAlg.CreateDecryptor(key, iv);
#Lavender:return result;
stop
@enduml
